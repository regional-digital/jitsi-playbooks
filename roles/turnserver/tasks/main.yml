---
- include_role:
    name: acme.sh

- name: install coturn
  apt:
    name:
      - coturn

- name: create certificate
  shell: /root/.acme.sh/acme.sh --issue --domain {{ turnservername }} --key-file /etc/ssl/private/{{ turnservername }}.key --fullchain-file /etc/ssl/certs/{{ turnservername }}.pem --reloadcmd "systemctl restart coturn" --standalone --listen-v4 {{ acme_sh_additional_options | default('') }} --force
  args:
    warn: false
    creates: /etc/ssl/certs/{{ turnservername }}.pem

- name: chmod&chown /etc/ssl/private/{{ turnservername }}.key
  file:
    state: file
    path: /etc/ssl/private/{{ turnservername }}.key
    owner: root
    group: root
    mode: '0750'
  notify:
    - restart coturn

- name: chmod&chown /etc/ssl/certs/{{ turnservername }}.pem
  file:
    state: file
    path: /etc/ssl/certs/{{ turnservername }}.pem
    owner: root
    group: root
    mode: '0660'
  notify:
    - restart coturn

- name: generate /etc/turnserver.conf
  template:
    src: turnserver.conf.j2
    dest: /etc/turnserver.conf
    owner: root
    group: root
    mode: "0644"
    backup: yes
  notify:
    - restart coturn

- name: generate /lib/systemd/system/coturn.service
  template:
    src: coturn.service.j2
    dest: /lib/systemd/system/coturn.service
    owner: root
    group: root
    mode: "0644"
    backup: yes
  notify:
    - reload coturn config
    - restart coturn
